# 实际运行验证报告

日期2026-09-09。环境为CPU，未安装transformers/peft，无CUDA。完整环境见results/pool_20260909/environment.json。

## 1. 测试

新增56项pytest通过；与用户上传原始基础包的110项联合，共166项通过（2.40秒，见pytest_final.log）。这个数量不代表重新运行全部E006冻结实验检查，也不构成推荐效果证据。

已实际完成：命令help、源码compileall、缓存提取/校验、字典拟合、显式选择计划、随机tiny因果LM训练线路。未执行：真实预训练权重、官方推荐数据、大目录生成评测、外部baseline复现或GPU加速。

## 2. 小型因果LM线路

随机初始化embedding+causal累积上下文+输出层，共48条人工记录。实际执行完整requires_grad梯度、固定投影、sklearn字典/OMP、选择与SFT。准入16条、32 response tokens；实际训练64次出现、128 response tokens。选中ID集合与训练消费集合一致，参数hash发生变化。该模型不是预训练LLM；不报告NDCG或榜单提升。

## 3. 80次受控线性训练

四个预定义情形×5 seeds×4方法，均288次实际训练更新。采用已知稀疏方向，因此不能作为“学习atoms比其他表示更好”的证据。数值是无噪声理想例子与错误标签反例，不是现实推荐收益预测。MSE越低越好。

| 情形 | 全池同更新预算 | 随机准入 | 完整方向覆盖 | 覆盖＋带符号目标 |
|---|---:|---:|---:|---:|
| scarce_balanced | 0.006363 | 0.006363 | 0.006363 | 0.006363 |
| rich_balanced | 0.009316 | 0.031005 | 0.006363 | 0.006363 |
| rich_redundant | 0.480093 | 0.561580 | 0.006363 | 0.006363 |
| rich_rare_wrong_labels | 1.315398 | 1.264954 | 3.073808 | 0.833333 |

稀缺情形中预算能够容纳全部记录，各选择器确实保留全部，结果相同。丰富重复情形说明固定训练次数可以因重复方向挤占而损失覆盖；这只是一个存在性例子。丰富但稀有方向全是错误标签时，纯覆盖变差。带符号目标使用了构造环境的精确clean-reference信息，真实任务不免费拥有这种信息，不能把该行改善当作可迁移保证。

所有运行及标准差在regimes.json中保存，没有丢弃反例或挑best seed。理想无噪声环境多个seed结果完全相同，不能由此声称算法在真实数据中零方差。

## 4. 当前证据级别

数学一致性：通过本轮测试覆盖的命题。程序线路：CPU随机tiny模型通过。真实任务数据可识别性：有来源和协议审计，未完成native实测。预训练LLM真实推荐效果：未验证。外部强baseline优越性：未验证。生产计算收益：未验证。
